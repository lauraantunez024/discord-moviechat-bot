// const {
//     SlashCommandBuilder

// } = require('discord.js')

// module.exports = {
//     data: new SlashCommandBuilder()
//         .setName('whatmovie')
//         .setDescription('What movie and where it at'),
//     async execute(interaction) {
//         const singleMovie = await movie.findOne({ where: { }})
//         await interaction.reply({ content: `We're watching ${movieData} and it's on ${streamingData}`, ephemeral: true })

//     }
// }